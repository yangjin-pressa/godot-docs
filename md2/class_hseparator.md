# HSeparator

**Inherits:** Separator < Control < CanvasItem < Node < Object

## Description
- A horizontal separator used for separating other controls that are arranged vertically.
- HSeparator is purely visual and normally drawn as a StyleBoxLine.

## Key Attributes
- **Virtual methods**: virtual (This method should typically be overridden by the user to have any effect.)
- **Const methods**: const (This method has no side effects. It doesn't modify any of the instance's member variables.)
- **Varargs**: vararg (This method accepts any number of arguments after the ones described here.)
- **Constructor**: constructor (This method is used to construct a type.)
- **Static methods**: static (This method doesn't need an instance to be called, so it can be called directly using the class name.)
- **Operator**: operator (This method describes a valid operator to use with this type as left-hand operand.)
- **Bitfield**: bitfield (This value is an integer composed as a bitmask of the following flags.)
- **Void**: void (No return value.)